# Open work inventory — 2026-09-12 → FINAL MATRIX (2026-09-14)

> **Status 2026-09-14 (Task 17): this is the final closeout matrix.** Every `AXS-*` row below is
> `CLOSED` (commit + evidence pointer), `DEFERRED` (concrete trigger), or `EXTERNAL` (owner +
> prepared command / trigger). Zero ambiguous rows. Studio HEAD at closeout: `902551d`
> (`closeout-2026-09-12`); arxa worktree HEAD: `3b6e08ea`. The authenticated/physical legs the
> operator has not authorized stay EXTERNAL — program law: no authenticated/physical leg is ever
> marked DONE without explicit per-leg authorization. Row-level detail for the Part B legs lives in
> [`closeout-evidence-2026-09-12.md`](closeout-evidence-2026-09-12.md) (the Task 16 matrix); their
> dispositions are carried in below without softening.
>
> Successor to [`open-work-inventory-2026-09-07.md`](open-work-inventory-2026-09-07.md). Every row of that
> read was re-verified against current source, tests, and git history on 2026-09-12 in the studio worktree
> `4d1b9241` (branch `closeout-2026-09-12`, `npm test` → `arxa-studio CI — 110 suites` / `ALL GREEN`) and the
> isolated sibling worktree `arxa` `5749402b` (`mobile_flutter`: `flutter analyze` → *No issues found*,
> `flutter test` → *All tests passed!*, +104).
>
> **ID note (2026-09-14):** Task 15 inserted two rows as AXS-042/AXS-043, colliding with the pre-existing
> CLOSED rows of those IDs (GitHub conversations live smoke; Z.ai wire-params handoff). The two newer rows
> are renumbered **AXS-052** (Windows) and **AXS-053** (runner passes); references fixed in
> `closeout-evidence-2026-09-12.md` (G10) and arxa `docs/linux-support.md`.

**Scope.** arxa studio (this repo) plus the two sibling-repo surfaces the program owns (`mobile_flutter` →
Task 12, `desktop/` → Task 15). The rest of the sibling `arxa` backlog is out of program scope and recorded
as EXTERNAL rows, not as open program work.

## CLOSED — program outcomes (Tasks 2–16, checked 2026-09-14)

| ID | Source plan | Outcome (commit + evidence) | Residue |
|---|---|---|---|
| AXS-001 | `org-trash-unreachable.md` (Bug B) | CLOSED — `8f10a08` `fix: stop organisation sessions before trash`; trash quiesce stops live sessions under the org (bounded, fail-closed) and preserves `org.json.name`; file-org-shell 228 checks + session-sweep + `scripts/org-purge-smoke.mjs` + `npm test` green (T2 review 2026-09-12) | T2 minors in ledger |
| AXS-002 | `git-card-sessions-worktree-rewire.md` D115 gap | CLOSED — `db6de5f` `feat: prepare imported projects for sessions`; `prepareProjectRepo`/`project.repair-repo` repair path + notice UI + one retry; smoke 10 repair checks green (T3 review 2026-09-12) | shell-CTA unborn-repo door = ledger minor |
| AXS-003 | `local-only-git-parity-and-sidebar-decorations.md` D3/D6/D7 | CLOSED — `7d9e866` `feat: surface local checks and complete tree decorations`; Local Checks row (cached, fingerprint-staled, 64 KiB cap) + Projects/project/stage rollups; card-local-smoke incl. red `check.sh` leg + drift `--check` green (T4 review 2026-09-13) | capGateOutput UTF-16 cap = ledger minor |
| AXS-004 | `local-only-git-parity-and-sidebar-decorations.md` S4/S5 | CLOSED — `82296f8` `test: cover main races and auto-commit storms`; S4 two-session race + S5 save storm as real child processes; found+fixed a real land-race index defect in `sessions.js` (bounded retry + `MERGE_HEAD`-gated heal); 5× cicd-stress green (T5 review 2026-09-13) | cicd-stress S5 load-flake = ledger row |
| AXS-005 | `github-conversations-in-the-insight-panel.md` | CLOSED — `bd14065` `feat: show the latest delivery ledger on the git card`; `card.ledger.summary` strip (persisted `next`, bounded view, trusted-URL link); ledger 12 checks + card-local-smoke 6c green (T6 review 2026-09-13) | live rerun → AXS-055 |
| AXS-006 | `artifact-viewer-implementation.md` (5 unchecked), `artifact-viewer-vscode-monaco.md`, `artifact-viewer-demo-runbook.md` | CLOSED — `78c1654` `fix: close artifact viewer runtime and trust boundaries`; trust boundaries (same-origin extension rule, SSE deny-default, token classes, tree-read mint), Monaco clean-build gate ×2 (`check.mjs`, `--webkit`), LSP last-socket scoping, live 1280 evidence (T7 review 2026-09-13) | gen-ui Diff real-input → AXS-065; D77 version bump → AXS-066; person-driven demo legs → AXS-068; fresh-boot LSP chain → AXS-063 |
| AXS-007 | `dsh-plugin-ui-conformance.md`, `palette-personalisation.md` | CLOSED — `5c2f83a` (en/pl/fr parity test + 29 keys + gate driver) + `f5e90d4` (capture script) + `d3a7c47` (narrow-lane fix) + `4347f07` (PL/FR native-review corrections, 5/5 must-fix) + `d1771af` (full L4 control ladder); evidence under `designs/evidence/studio-closeout/{390,744,1280}`; 130 suites ALL GREEN (T8 re-review 2026-09-14) | E2 dock lanes → AXS-064 |
| AXS-008 | `arxa-isolation-levels.md` S1–S4 (A0–A3) | CLOSED — `fd6fd5f` `feat: provision local confinement and project integrity`; effective-tier resolver, read/write-confined FileSystem provider, workspace-write preset seeding with provenance migration (A0), per-project age/SOPS secrets, integrity gates; s1-sandbox-verify real probes ALL PASS (sibling secret DENIED, sentinel unchanged) (T9 review 2026-09-13) | cell launcher seed → AXS-062 |
| AXS-009 | `arxa-isolation-levels.md` L1/A4 | CLOSED — `62ee8d5` + fix round `f35389b`; Docker detection, declarative devcontainer generation, volume-isolated lifecycle with host recovery refs (teardown data-loss guard disarmed→fixed in review), Compose-secret injection; devcontainer 35(+1 honest skip) green (T10 re-review 2026-09-13) | real-container leg → AXS-056 |
| AXS-010 | `arxa-isolation-levels.md` §§21–24/S3–S4 (A5) | CLOSED — `18c52cc` + fix round `e800fe9`; sbx packaging/provisioning, sign-in surface, clone/start/fetch/remove with `refs/sandboxes/<name>/<branch>` recovery, Finish/Sweep integration; sbx 32 + sbx-install 12/12 (measured SBX_PIN v0.42.1 digests, fail-closed install) green (T11 re-review 2026-09-13) | live sbx leg → AXS-015 |
| AXS-011 | `mobile-flutter-migration-spec.md` §a (12 boxes) | CLOSED — arxa `27878bcb` + `132f9003` (CI frame + release readiness) + `458f97e8` (pinned-sdk tool output) + `5930302d` (fix round: physical-gates.md etc.); studio `8e3bc01` (spec checklist closed); gates re-verified 2026-09-14: analyze 0 issues, flutter 108/108 incl. release-signing contract 4/4, kit dart 7 + rust 11, actionlint 4/4, `check.sh` red only at the inherited `gates` area (T12 re-review) | simulator smoke → AXS-060; release-shaped builds → AXS-061; physical legs → AXS-018 |
| AXS-012 | `agency-backend-provider-abstraction.md` (was PLAN ONLY) | CLOSED — Wire v1 frozen at `3597a40` (checkpoint CONTRACT STANDS), Part B `72a2a83`, fix round `4cc0380`; golden contract tests, `LocalWorkspaceProvider` zero-config default, generic REST adapter (bounded blobs), export/import, `provider verify`/`workspace export`/`workspace import`/`diagnose` CLI, packed-engine boot smoke wired into CI (126 suites) (T13 re-review 2026-09-13) | — |
| AXS-013 | `agency-backend-provider-abstraction.md` + Task 13 contract | CLOSED — `75d090f` `feat: add the first-party Supabase workspace provider` + fix round `ce5e82e` (Prefer semantics, deterministic smoke); Supabase adapter behind the frozen contract, RLS/IDOR-tested migrations (reversible; inverse SQL in-file), settings surface with truthful capability badges, CI `supabase-conformance` job; 129 suites green (T14 re-review 2026-09-13) | real stack leg → AXS-057 |
| AXS-014 | `desktop-shell-scaffold.md`, `linux-install-script.md`, `linux-omarchy-port.md` | CLOSED — arxa `fff96bee` + studio `2c74ab0` (2026-09-14, T15): entitlements + branded icon set + AppImage/deb/PKGBUILD `/usr/libexec` layout + systemd user unit + Wayland/HiDPI fixes + `.desktop`/icon install-uninstall + checksum/manifest generation verified on disk; `studio-v*` lane fail-closed (guard + post-build verify); `install-macos.sh` twin + hermetic test; notarization prep validated (profile itself = AXS-019); actionlint clean via `.github/actionlint.yaml` | Omarchy VM lanes → AXS-058; Windows → AXS-052; runner passes → AXS-053 |

## CLOSED — proven 2026-09-12 (source + tests; pre-program closures, unchanged)

| ID | Item | Evidence |
|---|---|---|
| AXS-033 | Project-repo routing (the `project-sessions-physical.md` design) | `plugins/git-workspace/lib/routing.js` DOCK_ROUTES (D98/D99; silent org fallback is B2 and refuses loudly), `sessions.js` org+project aggregation with repo-discovery preamble; commits `0df8eb8`, `209e5f7`, `d92a418`; `selftest.routing: 19/19 passed` |
| AXS-034 | Repo-less sessions / rows-face aggregation (D115 main body) | `d92a418`; the D115 *repair-path* gap closed by Task 3 (AXS-002) |
| AXS-035 | Version minting + `changes-requested` state | `plugins/git-workspace/lib/versions.js` (`mintVersion`, `VERSION_STATES` incl. `changes-requested`, `readTargetReleases`); `selftest.versions: 14/14 passed`. Still true 2026-09-14: the module is API-only by design; its first product trigger remains the arxa-side publish event (AXS-032) |
| AXS-036 | Project format stamp removal | Commit `a003287` "drop the project format stamp that was written, published and never read"; `plugins/workspace/lib/manifest.js:26` documents the stamp as derived-only |
| AXS-037 | Leaf/session/lister Git decorations | `decorations.js` `decorate`/`foldDirs`; D117 `session.decorations` in `arxa-sidebar/lib/client.js`; `selftest.decorations: ALL GREEN`. Top-level rollups closed by Task 4 (AXS-003) |
| AXS-038 | Claude subscription engine implementation | `plugins/claude-code/` full module set (models, probe, spawn, env, pending, mirror-tools, mirror-gate, bridge, handoff, mcp-bridge, approval, adapter, account, auth-flow, rate-limit, skill-packs, usage) with 21 selftests GREEN in the CI run; its plan's 88 unchecked boxes are stale tracking (banner 2026-09-12) |
| AXS-039 | Claude SDK packaging + skill-pack loading mechanism | `scripts/pack-list-check.mjs: 10 ok`; `@anthropic-ai/claude-agent-sdk 0.3.259` in the payload; `skill-packs.js` + selftest. The empty-default policy now also has an asserting test — `selftest.skill-packs.mjs` bundled-root-empty row, DONE 2026-09-14 (T16 G5a) |
| AXS-040 | GitHub authorization mounting | `plugins/github-link` device-flow mounting; `selftest.auth-flow: 7 ok` ("the authorization seam is mounted by a profile row and declared as a dependency") |
| AXS-041 | sbx research + login-boundary evidence | `arxa-isolation-levels.md` research sections (tiers, policy tables, concurrency finding); commits `9119642`, `30a7863`, `fd6c137`. Product integration closed by Task 11 (AXS-010); the login run itself → AXS-015 |
| AXS-042 | GitHub conversations live smoke (cicd card against a real repo) | `github-conversations-in-the-insight-panel.md`: "CLOSED 2026-09-03 — the authorised live smoke ran"; `2df41f0`, `ddffa17`, `19011e4` |
| AXS-043 | Z.ai wire-params capability handoff | `zai-wire-params-capability-handoff.md` Status: IMPLEMENTED 2026-08-29 (§4B override; §5 closed); battery PASS. Live smoke → AXS-017 |
| AXS-044 | Claude sign-in surface (Models page) | `claude-signin-surface-models-page.md`: live verification + closing note + follow-up grammar, all 2026-09-06; Sign out covered by `selftest.account` fake-spawner (live logout deliberately not run — it would kill the working CLI session); the stock Edit-form item is moot per the 2026-09-06 user direction ("no Edit") |
| AXS-045 | Claude sign-in/effort findings F11–F14 | `claude-signin-and-effort-selector.md` Outcome 2026-09-04: F12/F13/F14 fixed and verified live; F11 closed by `plugins/claude-code/package.json` + engine-sync content hash. F15 → AXS-025 |
| AXS-046 | Freestyle | `plugins/arxa-freestyle` shipped (org grammar parity, create/rename/publish/disconnect/sync/sweep/purge, decorations); 8 suites GREEN; commit series `a8e334d`→`5042258`, routing `64bf64a`; follow-ups → AXS-028 |
| AXS-047 | Dashboard restyle + installed-check | `10ea9df`, `82681e1`, `967b2cc`; 6 `arxa-dashboard` suites GREEN; dashboard-inside-open-chat → AXS-030 |
| AXS-048 | Palette consolidation + personalisation rows | `967b2cc` (one active-row style, ink wash); `theme-accent` + `personalisation` selftests GREEN; locale parity for the new keys closed by Task 8 (AXS-007) |
| AXS-049 | Client bundle diet | Measured not worth it (startup plan round 8) — declined by evidence; the remaining boot cost is upstream (AXS-020) |
| AXS-050 | File-org grill Q2 (Git repo boundaries in the tree) | Answered in practice by the D98/D99 routing table (AXS-033): projects/* → project repo, notes/meetings/communications → org repo, account → refused |
| AXS-051 | `org-trash-unreachable.md` reachability rescue | Superseded 2026-09-09 by `org-delete-trash-reachability.md` (gate steps aside; sidebar Trash reachable at zero orgs, commits `238a5d8`/`271bf87`). The live-session bug closed by Task 2 (AXS-001) |

## EXTERNAL — owned outside this program (owner + prepared command / trigger)

| ID | Owner | Item | Prepared command / trigger (verbatim where a command exists) |
|---|---|---|---|
| AXS-015 | operator (Docker account) | A5 live sbx leg (T16 G3) — OAuth device flow, reviewed global deny-all plan, disposable sandbox lifecycle, policy-denial proof, commit fetch to host recovery ref, removal, cache accounting; also reconciles the inferred `sbx ls --json` field shape | After per-leg authorization: `ARXA_A5_REAL_SMOKE=1 node plugins/sandbox/selftest.sbx.mjs` (after `sbx login`; reviewed global deny-all; disposable sandbox; recovery ref) |
| AXS-016 | operator (Claude subscription) | Claude signed-in smoke (T16 G5). Linux resume/Bash-escape rungs are N/A on macOS (recorded, not silently skipped). The empty-default skill-pack policy test itself is DONE (G5a) | After authorization: `node scripts/claude-code-smoke.mjs --yes` |
| AXS-017 | operator (Z.ai credential) | Z.ai live smoke (T16 G6) — one metered call, requires a real successful response with the intended model/wire params | `ZAI_API_KEY=<resolved by operator, never printed> node scripts/zai-live-smoke.mjs` |
| AXS-018 | operator (devices/credentials) | Mobile physical gates (T16 G8) — pair, reconnect, notification presentation/tap, approval decision, unpair/revocation; every leg incl. `PHASE=phone` real-APNs doorbell | Per-leg commands in `mobile_flutter/deploy/physical-gates.md` (arxa worktree, updated `3b6e08ea`); evidence dir `mobile_flutter/deploy/evidence/closeout-2026-09-12/` |
| AXS-019 | operator (Apple credentials) | Notarization + first signed `studio-v*` release. Prep validated 2026-09-14 (identity, entitlements, hardened runtime, updater key, notarytool command shape); keychain profile `arxa-notary` still absent; the app-specific password was never automated. The tag run itself proves signing+notarization (fail-closed guard) | After `xcrun notarytool store-credentials arxa-notary`: `bash desktop/scripts/sign-and-notarize.sh` (arxa worktree) + first live `release-linux` tag + clean-machine installer verify (T16 G9) |
| AXS-020 | upstream dsh | No resume verb until dsh has a contentless wake; no `job.*` RPC; dsh core boot ~1.1–1.2 s module I/O | Trigger: an upstream dsh release ships any of these; studio adapts on the `arxa update` pin-bump path |
| AXS-021 | sibling arxa repo / operator | Entitlement dev-key swap, Stripe products/webhook, `arxa login` PKCE + OAuth, self-service deactivation, holdco/opco + store accounts — sibling-repo commerce work | Sibling repo's own program; not studio work |
| AXS-022 | sibling arxa repo | Design-tool backlog (kit/cairn porting, engine LLM fabric, harness dictionary, fidelity-mode, showcase passes, enterprise tier); `designs/arxa-studio-v2` archived as abandoned | Sibling repo's own backlog; the studio-v2 items are superseded, not pending |
| AXS-053 | runner (self-hosted macOS ARM64) | First `desktop/**` PR → `desktop-boot-gate` + `linux-engine-unit` green on the runner; first `studio-v*`/`studio-beta-v*` tag → full `release` + `release-linux` pass (signed pass rides AXS-019). **Then branch protection per `docs/ci/setup.md` contexts** (T16 G11: operator pushes branch → runner executes `check.sh`). Workflows actionlint-clean, guard logic proven standalone 2026-09-14; neither job has ever run | Trigger: operator pushes the branch / opens the first `desktop/**` PR / pushes the first release tag; then branch protection after the first green run |
| AXS-055 | operator (GitHub quota) | card-cicd-smoke rerun (T6 residue, T16 G1) — the 6 T6 failures were operator private-repo Actions minutes exhausted (external quota; source-audited, `bd14065` not implicated) | Trigger: Actions minutes restored + per-leg authorization → `node scripts/card-cicd-smoke.mjs --yes` |
| AXS-056 | operator (Docker daemon) | A4 real-container end-to-end leg (T16 G2); injected-runner coverage already pins the docker argv surface | Trigger: operator starts the Docker daemon (never the agent) → `ARXA_A4_REAL_SMOKE=1 node plugins/sandbox/selftest.devcontainer.mjs`; watch `row.head` reachability mid-fetch (`plugins/sandbox/lib/devcontainer.js:456`) |
| AXS-057 | operator (Docker daemon) | Supabase real stack leg (T16 G4): SQL/migrations/Prefer live behavior + `status -o env` key-name verification on first live run **+ member-gated org DELETE RLS policy verification (T14 ledger minor folded here per whole-branch review F5)**; the CI `supabase-conformance` job also carries it | Trigger: operator starts the Docker daemon → `node scripts/workspace-provider-supabase-smoke.mjs --real` |
| AXS-058 | operator (VM + daemon) | Omarchy VM user path + Arch/Ubuntu container lanes (T15 degraded honestly: docker down by operator choice, 8 GB VM floor, disk pressure; T16 G10) | Trigger: operator VM + daemon → runbook + assertions in arxa `docs/linux-support.md`; container lane `DISTRO=ubuntu scripts/linux/run-container.sh` |
| AXS-059 | arxa repo (outside this program) | `arxa gate --all` exit 1 = INHERITED pre-branch debt: intake/coverage studio-design drift + advertise digests stale since the pre-branch H7 rename (`73bae180`, ancestor of the program base) + deploy `approvalTokens` (deliberate human gate) + review/lens env skips. Branch diff ∩ gate inputs = ∅. First CI `gates` area runs red until this is separately resolved (visible on the AXS-053 runner pass) | Trigger: the arxa repo's own remediation of the `gates` area; owner = arxa maintainers, not this program |
| AXS-062 | whole-program review (plan Step 7) | `bin/.arxa-cell-launcher.mjs:210` still seeds `permission.defaultPreset: danger-full-access` — a second launcher variant; if the cell launcher is a live boot path, every home it seeds stays A0. Not shipped packed (not in BIN_FILES) | AWAITS Step 7 adjudication — candidate fix: route through `bin/seed-settings.mjs`'s seedPermissionPreset (ledger T9 finding) |
| AXS-063 | artifact-viewer owner (outside this branch) | L1/L2 viewer LSP chain dead on fresh boots (`plugins/artifact-viewer/lib/index.js:183-191`): token→`/lsp/status`→ws 403s without the person-pressed install door; no ts/css/json squiggles, dart-absent strip never renders (`strip:false, editor:true`, light+dark shots). REPRODUCED at HEAD and identical at BASE `4d1b924` — the only pre-existing-at-BASE escalated finding; the committed 2026-09-13 §7 evidence stands as richer-env evidence, marked env-coupled | Trigger: any fresh scratch boot reproduces; product fix owned by the artifact-viewer owner. AWAITS Step 7 adjudication (T16 E1) |
| AXS-064 | arxa-git-card + dsh conversation-input owners | Fresh-session conversation dock dead on fresh boots at every width (T8 RIDE ruling honored): session creates, opens, binds, composer activates — `conversation.input.dock` never renders, no `[data-git-dock]`; finish/checks-red/confinement recorded as FAILURE-STATE rows with 1280 controls. Counter-evidence: the operator's installed studio mounts docks daily | Trigger: product fix by the named owners. AWAITS Step 7 adjudication (T16 E2); branch must not close on stale dock evidence |
| AXS-067 | operator | Phase 0b destructive data cleanup (session-execution-log) — snapshots taken 2026-09-01 (3 files under `docs/plans/phase0b-snapshots/`); the cleanup itself **explicitly requires authorization; not touched** | Trigger: explicit operator authorization per the Phase 0b plan |

## DEFERRED — recorded with concrete triggers

| ID | Source plan | What is deferred | Concrete trigger (revisit when) |
|---|---|---|---|
| AXS-023 | `artifact-viewer-vscode-monaco.md` phases 4–5 | autoSave→VS Code setting ownership change, PDF extension experiment, Prettier→VS Code formatter swap (program rulings 3–4: pdf.js + Prettier + the tested 1.5 s debounce are retained) | A replacement candidate passes the feature-parity tests |
| AXS-024 | `artifact-viewer-docked-column.md` T4 anchor | Files rows over the T3 tree route in the sidebar files section (held 2026-09-07) | The sidebar files-surface grill that owns that surface lands and rules the row grammar |
| AXS-025 | `claude-signin-and-effort-selector.md` F15 | Probe swap to `claude auth status` for the liveness check (deliberate, recorded 2026-09-04: "F15 not done") | The SDK probe's cost is measured as a real problem |
| AXS-026 | `sidebar-org-rethink.md` | The *direct* delete-org verb (trash/purge is the shipped path) | The operator asks for direct delete |
| AXS-027 | `mobile-flutter-migration-spec.md` Q6 | Biometric approvals (no contract exists in any doc) | The operator sets v1 scope AND a written approval-biometrics contract lands in `mobile_flutter/docs` (owner: mobile surface) |
| AXS-028 | Freestyle follow-ups | Multi-select, clipboard, compact folders, file nesting, sort — each its own verb; none is an unchecked box elsewhere | Any one verb: the operator names it after the 2026-09-14 closeout (owner: freestyle surface) |
| AXS-029 | `artifact-viewer-implementation.md` | Viewer edit-with-agent (agent edits the open file → conflict prompt beyond the runbook leg) | An agent-edit contract with conflict-UX budget is written in `docs/plans` and names the viewer as consumer (owner: artifact-viewer) |
| AXS-030 | Dashboard follow-ups | Dashboard-inside-open-chat | The operator requests it after the 2026-09-14 closeout (owner: dashboard surface) |
| AXS-031 | `mobile-flutter-migration-spec.md` Q7 | Archiving/removing the legacy Tauri scaffold (`arxa/mobile`) | First accepted iOS **and** Android store releases land (mobile_flutter supersedes it; Task 12 retained it until then) |
| AXS-032 | `arxa-studio-vocabulary-collisions.md` V5 | Version-minting ledger shape (project ledger + per-target rows) + arxa-side `supersededBy`. **Outcome recorded 2026-09-14 (Task 17): NOT ACCEPTED, NOT ADDED** — no arxa-side governing design/version contract carries a `supersededBy` field (grep of the arxa worktree at `3b6e08ea`: zero matches; `docs/VOCABULARY.md` has no such term). Studio's half is built (AXS-035); no unconsumed field was added on either side | The sibling arxa repo's design/version contract explicitly adopts (or rejects with rationale) the ledger shape and `supersededBy`; studio adapts on that decision |
| AXS-052 | D23 (`../arxa/docs/plans/distribution-and-platforms.md`); research: `../arxa/docs/research/windows-packaging.md` | Native Windows distribution — every piece of it (sidecars, Credential Manager, folder picker, service, installer, workflow, PowerShell installer). Research exists (D34, MSIX/winget/Trusted Signing); research is evidence, never authorization to build | The exact D23 trigger fires: a sustained demand signal — a support/waitlist volume threshold — or a Scale customer requiring local/offline builds the hosted web version cannot serve |
| AXS-054 | T15 finding (installer channel) | `install-macos.sh` preserves the channel at the INSTALLER level only (`~/.config/arxa-studio/channel`); the in-app updater follows `ARXA_UPDATE_CHANNEL` (env), so a beta installed via the installer still checks the stable feed in-app unless the env is set. Fix (src-tauri reading the channel file) was deliberately outside the program's file list; README documents the divergence | First src-tauri change that touches the updater/channel read path (owner: arxa desktop; fix = shell reads `~/.config/arxa-studio/channel`) |
| AXS-060 | T12 Step 5 (T16 G12) | Simulator integration smoke (RAM-discipline deferral: two OOM kills on 2026-09-14 set the default; a later RAM-quiet run may execute it without new authorization — scratch-safe class) | An idle-machine window; then verbatim: `xcrun simctl boot "iPhone 16"` (disposable sim) → `cd mobile_flutter && fvm flutter test integration_test/approvals_e2e_test.dart -d "iPhone 16" --dart-define=PHASE=smoke` (scratch engine boot per `mobile_flutter/deploy/physical-gates.md` §0) |
| AXS-061 | T12 Step 8 (T16 G12c) | Release-automation builds without publishing (same RAM-discipline class as AXS-060) | An idle-machine window; then verbatim: disposable-keystore `keytool -genkeypair …` → `fvm flutter build appbundle --release` + `fvm flutter build apk --release` → `cd ios && fastlane ios doctor` (STOPS THERE — no submit); keystore destroyed after (full command in `closeout-evidence-2026-09-12.md` §G12-notes) |
| AXS-065 | `artifact-viewer-implementation.md` Task 9 | gen-ui Diff card: swap line-by-line for the merge view when a real file diff lands (the LCS retirement note; a DEFERRED marker ships in the card) | The first gen-ui consumer feeds real file-diff input into the card (owner: artifact-viewer/gen-ui; keep the card's data contract) |
| AXS-066 | `artifact-viewer-implementation.md` Task 12 (D77) | D77 plugin version bumps for the program's shipped plugin changes — no touched plugin's `package.json` version was bumped on the branch (e.g. artifact-viewer stayed `0.3.5` through T7's trust-boundary work, its own acceptance box naming the bump). D77's lesson: pnpm profile materialization may not re-copy a plugin whose version string is unchanged | The first release/pack of this branch: bump every touched plugin's version per D77 and re-run `npm test` + pack checks (owner: release owner) |
| AXS-068 | `artifact-viewer-demo-runbook.md` §§1–§5 | Person-driven demo legs (dock/drag/maximize/sheet/session-switch) — interactive by design; no authorization needed, just a person (T16 G7-notes: PREPARED) | The operator walks the runbook §§1–§5 on an installed studio; evidence lands under `designs/evidence/studio-closeout/` |
