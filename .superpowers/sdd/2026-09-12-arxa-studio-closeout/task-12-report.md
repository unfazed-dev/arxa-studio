# Task 12 report — Reconcile and finish the Flutter mobile deliverable

## Part A

**Commits** — arxa (closeout worktree) `132f9003` `feat: reconcile mobile ci and release readiness` (15 files, +1261/−3); studio `8e3bc01` `docs: close the mobile migration checklist`. No push/tag/release.

**Files created per skill template** (arxa-cicd): `scripts/check.sh` (one-root: `arxa gate --all` wrapped + fvm flutter format/analyze/test + commits/pr-title/mobile/mobile-signing/transport areas, green by absence), `.github/workflows/ci.yml` (six job-per-area lanes, hosted Ubuntu, per-job timeouts, concurrency cancel), `.github/pull_request_template.md` ([arxa-<skill>] stage field + SSOT tag map footer), `docs/ci/{decisions,setup}.md`, `docs/ci/explainer.html` (template tokens filled, both readings). (arxa-deployer): `.github/workflows/mobile-release.yml` (prepare-then-halt; AAB release-shaped with real keystore or disposable TEST-SIGNED one; unsigned iOS archive + identity doctor; never uploads), `mobile_flutter/fastlane/{Appfile,Fastfile,Matchfile}` (operator-run TestFlight/App Store/Play internal+production lanes, clear credential-presence failures, no checked-in secrets), `mobile_flutter/shorebird.yaml`, `mobile_flutter/deploy/README.md` (readiness facts), `mobile_flutter/evidence/parity-2026-09-14.md` (Step 2 table, adopted from prior attempt with box-12 smoke row corrected to pending), `mobile_flutter/test/android_release_signing_contract_test.dart` (prior attempt's, verified against brief).

**Decisions recorded** (docs/ci/decisions.md, dated 2026-09-14, pre-made per brief): private monorepo; main trunk; hosted-Ubuntu mobile gate lanes (recorded exception to self-hosted-runners.md, with why); iOS on [self-hosted, macOS, ARM64, arxa]; per-job timeouts; concurrency cancellation (release lane deliberately not); `arxa gate --all` + flutter format/analyze/test wrapped not duplicated; deploy prepare-then-halt (HUMAN GATE 3); pr-title warn-not-fail (flip = future recorded decision); no paths filters (strict-contexts safety); branch protection applied only after first green watched.

**Signing edits** — build.gradle.kts: prior attempt's Step-4 edit adopted after verification (gitignored `android/key.properties`/CI secrets, all-four-inputs guard, clear GradleException on release packaging without inputs, debug key only for debug builds; `applicationId = solutions.arxadigital.arxa.mobile` unchanged; key.properties already gitignored). iOS: no tree edit needed — team `43GNRCGQXQ`, bundle `solutions.arxadigital.arxa.mobile`, automatic signing confirmed against pbxproj; release signing match-owned with export options carried in Fastfile; `aps-environment: development` flip documented.

**OTA judgment** — extension point CREATED DORMANT (empty `app_id`): the migration spec has zero OTA rows (M5 = TestFlight + APK sideload), but inventory row AXS-011 names Fastlane/Shorebird as this task's closure work; activation is operator action (`shorebird create-app`), documented in the file.

**Syntax validations (no toolchains)** — `sh -n` OK; `yaml.safe_load` OK (ci.yml, mobile-release.yml, shorebird.yaml); `ruby -c` OK (Fastfile/Appfile/Matchfile); `actionlint` clean after one SC2015 fix; `check.sh mobile-signing` and `commits` areas run green (pure shell); pr-title skips locally as designed.

**Part B must run** — Step 1 re-prove (expect fvm 3.44.9 + New-finding-1 mkdir workaround); `check.sh gates/mobile/transport` for real; `flutter test mobile_flutter` incl. `test/android_release_signing_contract_test.dart` (RED-never-run: prior attempt's, not executed here); `arxa deploy --self-test`; Step 5 simulator integration smoke; Step 8 release-shaped AAB with disposable keystore + iOS doctor stop (parity table box-12 row and spec box 12 cite these as pending); Step 9 `deploy/physical-gates.md`; watch first ci.yml run green, then apply branch protection (contexts in docs/ci/setup.md).

**Disclosures** — (1) Sibling worktree held uncommitted WIP from the OOM'd attempts beyond the Step-4 gradle edit: a lib/data→lib/services repository refactor (31 status entries) left UNCOMMITTED and untouched; my first commit accidentally swept in three pre-staged renames (they were already in the index) — caught via `git grep` at HEAD (8 files import the old paths), reset, recommitted clean. (2) HEAD of the worktree was `27878bcb` (prior attempt's Step-1 hygiene commit), above the dispatched base `fff96bee`; parity table audited at that commit. (3) iOS lane never ran (no toolchain) — self-hosted runner fitness is Part B/AXS-043 territory.

## Part B

**Status** — complete. Gates run RAM-disciplined (one at a time, GRADLE_USER_HOME/CARGO_TARGET_DIR redirected into worktree `.cache/`); WIP adjudicated; no push/tag/release.

**WIP adjudication — REVERTED.** The uncommitted `lib/data`→`lib/services` refactor (31 entries: repository/widget moves to untracked destinations + kit formatting + lock re-pins; the −599 is the three moved files counting as pure deletions) is demanded by nothing: the migration-spec has zero `lib/data`/`lib/services` rows, Part A's report/import map does not reference it, 0 committed files import the new destinations, and it edits `kit/studio_transport` sources that Ruling 10 fences off. Reverted via `git checkout -- .` + path-scoped clean; tree clean except scratch `.cache/`.

**Gate results**
1. `mkdir -p mobile_flutter/build/ios/SourcePackages` — ok (Task 1 lore).
2. mobile_flutter: format check caught 1 drifted file (Part A's signing contract test) → formatter applied, committed; `flutter analyze` — No issues found; `flutter test` — **108/108 GREEN** including `android_release_signing_contract_test.dart` (focused run: 4/4, "missing signing inputs fail a release build clearly" ✓).
3. transport: kit/studio_transport **7/7 dart GREEN** + rust `cargo test --lib` **11/11 GREEN**.
4. `arxa gate --all` (`dart run bin/arxa.dart` from the worktree, the AGENTS.md/check.sh form): **EXIT 1 — inherited red**, identical-or-worse on canonical main (canonical additionally fails `structure`). Failure taxonomy: intake FAIL 42 (studio registry↔brief surface drift), coverage FAIL 1 (design producer input missing; `KIT_DESIGN_DIR` does not rescue), advertise FAIL (tier1/deploy suite digests stale since pre-branch H7 rename `73bae180`), deploy FAIL 2 (approvalTokens version/account unconfirmed — the deliberate human release gate), review/lens env-not-applicable. None are mobile parity rows → reported, not fixed (Ruling 10 + brief scope). tests/structure/scaffold/freeze/memory/kind_registry/native_deps PASS.
5. `bash scripts/check.sh` end-to-end once: commits ok · pr-title skip (local, by design) · gates FAIL (inherited, above) · mobile ok · mobile-signing ok · transport ok (dart+rust) — RED only at the gates area.
6. actionlint: ci.yml + mobile-release.yml CLEAN (desktop-gate.yml + desktop-release.yml also clean).
7. studio `npm test` once: **EXIT 0, ALL GREEN — 130 suites**, zero fail matches.

**analysis_options/lockfile decision** — committed as the tool's canonical output (commit `458f97e8`): dart-format reflow of the contract test under fvm 3.44.9, and flutter-recorded analyzer excludes (`build/android/ios/macos`) in the kit. The predicted 5-entry pub-lock re-pin did NOT recur — locks already settled by `27878bcb`; kit `pubspec.lock` untouched after the revert.

**Commits** — arxa `458f97e8` `style: settle pinned-sdk tool output in mobile and transport kit` (+49/−28, Co-Authored-By trailer). Studio: none needed (report append only, uncommitted scratch per SDD convention).

**Concerns** — (1) `arxa gate --all` red is repo-wide pre-existing debt (intake/coverage/advertise/deploy-tokens); CI's gates area will be red on first push until resolved separately — advertise's own prescribed remedy (re-run the suite to re-record) predates this branch and touches studio-design evidence ledgers, so left alone per scope. (2) Studio worktree carries pre-existing `M package-lock.json` (+2 lines, npm metadata drift from the Part A session) — disclosed, not committed. (3) Simulator smoke (Step 5), release-shaped AAB/iOS doctor (Step 8), `arxa deploy --self-test`, and branch protection remain unexecuted — outside this Part B's dispatched gate list and barred here by the no-push/no-release rulings.

## Fix round 1

Doc-only round for the five review findings (2 Important, 3 Minor), 2026-09-14. Worktree `arxa-closeout`, single commit `5930302d` `fix: close the mobile release documentation gaps` on `closeout-2026-09-12` (base `458f97e8`), 5 files +126/−8, Co-Authored-By trailer. No push/tag/release.

1. **Important 1** — wrote `mobile_flutter/deploy/physical-gates.md`: execution-gated banner (devices + credentials, owner Task 16); iOS prerequisites (iPhone + team `43GNRCGQXQ` dev profile + `security cms -D` aps-environment check + fastlane `ios doctor`), Android prerequisites (Play-services device + bring-your-own `google-services.json`, never committed); real pairhost/iroh harness commands (LAN default, `ARXA_PAIRHOST_RELAY=1` cellular, `TICKET` single-use re-mint, `python3 -m http.server 8899` ticket server); per-leg table (pair, reconnect incl. `CELLULAR`, push registration, notification presentation + tap-through, approval decision, conversation send, unpair/revocation); e2e invocation shapes (`PHASE=smoke|e2e|pressure`, `PAIR_TICKET` vs `TICKET_URL`); APK leg (`adb install` + logcat); evidence dir `evidence/physical-gates-<date>/` + redaction rules (sed strips `arxa-pair:` tickets, 64-hex tokens, UDIDs; presence-only credential checks; no QR payload in screenshots). Points at `deploy/README.md` for fastlane/signing/CI instead of duplicating. README's dangling "a separate step writes `physical-gates.md`" line (37-38) now points at the file; EOF blank line gone (Minor 3 closed in the same edit).
2. **Important 2** — `test/android_release_signing_contract_test.dart` header no longer cites nonexistent `evidence/release-smoke-2026-09-14.md`; live signing smoke named PENDING (external, credential-gated, Task 16) with the contract test as the interim guard. Comment-only.
3. **Minor 3** — `deploy/README.md` trailing blank line at EOF removed; `git diff --check` now clean.
4. **Minor 4** — `mobile-release.yml` disposable test keystore dname: `C=AU` omitted (neutral per Ruling 11 spirit — committed artifact).
5. **Minor 5** — Fastfile ios `doctor` lane: `match_cert_count` → `provisioning_profile_count` (it counts `*.mobileprovision` files); message text was already correct.

**Verification** (RAM, one toolchain at a time, `PUB_CACHE`/scratch in worktree `.cache/`, `build/ios/SourcePackages` mkdir applied): `git diff --check` clean pre-commit and `git show --check` clean on `5930302d`; `fvm flutter analyze` — No issues found; focused `fvm flutter test test/android_release_signing_contract_test.dart` — **4/4 All tests passed**; `actionlint .github/workflows/mobile-release.yml` — clean; `ruby -c mobile_flutter/fastlane/Fastfile` — Syntax OK.
