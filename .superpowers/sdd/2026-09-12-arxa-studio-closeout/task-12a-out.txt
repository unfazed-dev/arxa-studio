⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
[claude-code:unrecognized_model] {"model":"glm-5.3[1m]","query_source":"sdk"}
**Status:** Part A complete — zero toolchain runs, all validations green.

**Commits:**
- arxa (`arxa-closeout`, on `27878bcb`): `132f9003` `feat: reconcile mobile ci and release readiness`
- studio: `8e3bc01` `docs: close the mobile migration checklist`
- Report: `.superpowers/sdd/2026-09-12-arxa-studio-closeout/task-12-report.md` → `## Part A`

**Files:** 15 — check.sh, ci.yml, mobile-release.yml, PR template, docs/ci/{decisions,setup,explainer}, fastlane trio, shorebird.yaml, deploy/README, parity table + signing contract test, build.gradle.kts.

**OTA:** created DORMANT — spec has zero OTA rows, but AXS-011 names Shorebird in closure work; activation is operator-side.

**Syntax:** sh -n ✓ · yaml.safe_load ×3 ✓ · ruby -c ×3 ✓ · actionlint ✓ (one SC2015 fixed) · mobile-signing + commits areas ran green.

**Concerns:**
- Worktree held prior attempts' WIP: a lib/data→lib/services refactor (31 entries) — left uncommitted; my first commit accidentally swept 3 pre-staged renames (would've broken HEAD — 8 files import old paths), caught + reset + recommitted clean.
- `android_release_signing_contract_test.dart` never executed (Part B).
- Branch protection deliberately NOT applied — after first green run is watched (checklist in setup.md).
