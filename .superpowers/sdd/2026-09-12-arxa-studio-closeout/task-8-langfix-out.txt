⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
[claude-code:unrecognized_model] {"model":"glm-5.3[1m]","query_source":"sdk"}
✅ **Status:** done — all must-fixes + safe nice-to-fixes landed, committed, report appended

📦 **Commit:** `4347f07` `fix: apply native-review locale corrections` (7 files; package-lock dirt excluded; report is scratch/gitignored)

🔢 **Counts:** must-fix 5/5 landed (av lsp strip ×en/pl/fr, pl wakeStarted, fr parked+wakeStarted) · nice-to-fix 10 landed / 15 skipped (tone/meaning rewrites, overflow risks — reasons in report §Step 3)

🧪 **Tests:** parity + av/git-card/sidebar/locale selftests green, gen `--check` in sync, `npm test` exit 0 — 130 suites ALL GREEN

⚠️ **Concerns:** sidebar selftest pinned old `initRepo` fr casing — pin updated to corrected copy (intent unchanged); artifact-viewer `CodeView` now takes `t` prop (was the only view without it); no push performed
