⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
[claude-code:unrecognized_model] {"model":"glm-5.3[1m]","query_source":"sdk"}
**Status:** ✅ done — settings prelude scoped (only `personalisation` opens settings; theme flips at narrow ride `prefers-color-scheme` emulation, the theme service's own system path), padEnd crash fixed (`classify` returns `kind`), rail fold + first-run notice handled, ladder run end-to-end.

**Commit:** `d3a7c47 docs: complete the narrow-width evidence ladder` (37 files; `package-lock.json` dirt untouched; task-8-report Part B + 5 ledger findings updated on disk — `.superpowers/` is git-excluded by design).

**Ladder:** exit 0 per contract · 390: 3 ok / 10 limitation rows / 2 hard (trash pair wedged→480s budget kill, non-proven) · 744: 2 ok / 11 limitation / 0 hard.

**Personalisation:** CAPTURED light+dark at BOTH 390 and 744 — dialog mounts fine (342px/696px panels); the "never mounts at narrow" was a trigger-discovery bug (fixed: real trigger = `button[aria-haspopup="dialog"]`).

**Concerns:** ① LSP chain dead on fresh boots at ALL widths (probe-verified identical at 1280): no ts/css/json squiggles, dart strip never renders (editor mounts) — limitation rows L1/L2 + ledger. ② `wp.info()` connection error at all widths (L3, regression vs T8-era evidence). ③ Session surfaces (finish/checksred/confine): conversation pane never mounts at narrow (hero stays) — L4, no 1280 provenance. ④ New filtered console noise: `missing #root`+favicon (2nd-browser re-bootstrap, bisected) and failing dart LSP ws — both justified in IGNORED.
