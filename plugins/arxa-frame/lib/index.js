// Host loader entry for the browser-only arxa-frame plugin (D88-D93).
// Provides no host-side behavior: the frame is the generated client half
// (lib/client.js, from scripts/gen-frame.mjs), discovered via dsh.client.
function apply() {}
export { apply };
