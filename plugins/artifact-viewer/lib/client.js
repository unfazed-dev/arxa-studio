// Browser half of arxa-artifact-viewer — Task 1 placeholder.
// The render lanes (direct / sandboxed iframe / stream), the CodeMirror 6
// editor, badges, and live color state arrive with Tasks 4-11 of
// docs/plans/artifact-viewer-implementation.md. Registers nothing yet, so the
// dsh.client discovery has a real module to load from day one.
export const name = 'arxa-artifact-viewer'
