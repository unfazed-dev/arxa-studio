// Host half of arxa-theme-accent — intentionally empty. The accent choice is
// pure client state (localStorage + CSS custom properties); the browser half
// is discovered through package.json dsh.client, same as the design panel.
export const name = 'arxa-theme-accent'

export function apply() {}
