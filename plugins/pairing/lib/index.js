// Host half of arxa-pairing — intentionally empty. Pairing lives in the
// desktop shell (Rust: open_pairing_window + pairing.rs); the browser half
// is discovered through package.json dsh.client, same as the design panel.
export const name = 'arxa-pairing'

export function apply() {}
